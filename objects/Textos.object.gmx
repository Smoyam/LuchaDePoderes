<!--This Document is generated by GameMaker, if you edit it by hand then you do so at your own risk!-->
<object>
  <spriteName>&lt;undefined&gt;</spriteName>
  <solid>0</solid>
  <visible>-1</visible>
  <depth>-100</depth>
  <persistent>0</persistent>
  <parentName>&lt;undefined&gt;</parentName>
  <maskName>&lt;undefined&gt;</maskName>
  <events>
    <event eventtype="0" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>textoPantalla = 0;
mostrar_texto = false;
cuadrosCreados = false;

// DIALOGOS
estadoDialogo = 0;
// Lucia
textoLucia_1 = "¿Que estas leyendo?"
textoLucia_2 = "Sii, ahora voy"

// Corsi
textoCorsi_1 = "Hola jajaja"
textoCorsi_2 = "Se llama «Mi Batalla»#del autor H.A."
textoCorsi_3 = "Jose Javier te esta#esperando ¿no?"

// Jose Javier
textoJoseJavier_1 = "¿Nos vamos ya?"

// Angie
textoAngie_1 = "ZzZzZz"
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="3" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>// Variable para el número de letras a mostrar
if (keyboard_check_released(ord("A")) &amp;&amp; cuadrosCreados == false)  {
    if (position_meeting(312,208,Lucia) || (position_meeting(112,224,Lucia)) || position_meeting(664,336,Lucia)) {
        mostrar_texto = true;  // Activa la bandera para mostrar el texto
        letras_mostradas = 0;  // Inicializa el número de letras mostradas
    }
    audio_play_sound(pasarTexto, 1, false);
} else if (keyboard_check_released(ord("A")) &amp;&amp; cuadrosCreados == true)  {
    mostrar_texto = false;
    cuadrosCreados = false;
    with (Caras) {instance_destroy();}
    with (CuadroTextoObj) {instance_destroy();}
    if (position_meeting(112,224,Lucia)) {
        estadoDialogo += 1;
    }
}

if (mostrar_texto) {
    //JOSE JAVIER 1
    if (position_meeting(312,208,Lucia)) {
        // Incrementa las letras mostradas gradualmente
        if (letras_mostradas &lt; string_length(textoJoseJavier_1)) {
            letras_mostradas += 1.0; // Ajusta este valor para controlar la velocidad
        }
        if (cuadrosCreados == false) {
            cuadrosCreados = true;
            // Dibujar el recuadro
            instance_create(view_xview[0]+220, view_yview[0]+160, CuadroTextoObj);
        
            with (instance_create(view_xview[0]+60,view_yview[0]+160,Caras)) {
                sprite_index = caraJoseJavier;
            }
            audio_play_sound(dialogoM0, 1, false);
        }
    // CORSI
    } else if (position_meeting(112,224,Lucia)) &amp;&amp; (estadoDialogo &lt; 5) {
        if (estadoDialogo == 0) {
            // Incrementa las letras mostradas gradualmente
            if (letras_mostradas &lt; string_length(textoCorsi_1)) {
                letras_mostradas += 1.0; // Ajusta este valor para controlar la velocidad
            }
        } else if (estadoDialogo == 1) {
            // Incrementa las letras mostradas gradualmente
            if (letras_mostradas &lt; string_length(textoLucia_1)) {
                letras_mostradas += 1.0; // Ajusta este valor para controlar la velocidad
            }
        } else if (estadoDialogo == 2) {
            // Incrementa las letras mostradas gradualmente
            if (letras_mostradas &lt; string_length(textoCorsi_2)) {
                letras_mostradas += 1.0; // Ajusta este valor para controlar la velocidad
            }
        } else if (estadoDialogo == 3) {
            // Incrementa las letras mostradas gradualmente
            if (letras_mostradas &lt; string_length(textoCorsi_3)) {
                letras_mostradas += 1.0; // Ajusta este valor para controlar la velocidad
            }
        } else if (estadoDialogo == 4) {
            // Incrementa las letras mostradas gradualmente
            if (letras_mostradas &lt; string_length(textoLucia_2)) {
                letras_mostradas += 1.0; // Ajusta este valor para controlar la velocidad
            }
        }
        if (cuadrosCreados == false) {
            cuadrosCreados = true;
            // Dibujar el recuadro
            instance_create(view_xview[0]+220, view_yview[0]+160, CuadroTextoObj);
            
            if (estadoDialogo == 0) || (estadoDialogo == 2) || (estadoDialogo == 3) {
                with (instance_create(view_xview[0]+60,view_yview[0]+160,Caras)) {
                    sprite_index = caraCorsi;
                }
                audio_play_sound(dialogoM0, 1, false);
            } else if (estadoDialogo == 1) {
                with (instance_create(view_xview[0]+60,view_yview[0]+160,Caras)) {
                    sprite_index = caraLucia;
                }
                audio_play_sound(dialogoF0, 1, false);
            } else if (estadoDialogo == 4) {
                with (instance_create(view_xview[0]+60,view_yview[0]+160,Caras)) {
                    sprite_index = caraLucia;
                    image_index = 1;
                }
                audio_play_sound(dialogoF0, 1, false);
            }
        }       
    } else if (position_meeting(664,336,Lucia)) {
        // Incrementa las letras mostradas gradualmente
        if (letras_mostradas &lt; string_length(textoAngie_1)) {
            letras_mostradas += 1.0; // Ajusta este valor para controlar la velocidad
        }
        if (cuadrosCreados == false) {
            cuadrosCreados = true;
            // Dibujar el recuadro
            instance_create(view_xview[0]+220, view_yview[0]+160, CuadroTextoObj);
        
            with (instance_create(view_xview[0]+60,view_yview[0]+160,Caras)) {
                sprite_index = caraAngie;
            }
        }
    }
}
</string>
          </argument>
        </arguments>
      </action>
    </event>
    <event eventtype="8" enumb="0">
      <action>
        <libid>1</libid>
        <id>603</id>
        <kind>7</kind>
        <userelative>0</userelative>
        <isquestion>0</isquestion>
        <useapplyto>-1</useapplyto>
        <exetype>2</exetype>
        <functionname></functionname>
        <codestring></codestring>
        <whoName>self</whoName>
        <relative>0</relative>
        <isnot>0</isnot>
        <arguments>
          <argument>
            <kind>1</kind>
            <string>if (mostrar_texto) {
    // Configurar la fuente antes de dibujar el texto
    draw_set_font(TextosFonts);  // Usa la fuente que creaste    
    
    // JOSE JAVIER
    if (position_meeting(312,208,Lucia)) {
        var texto_parcial = string_copy(textoJoseJavier_1, 1, floor(letras_mostradas));
    // CORSI
    } else if (position_meeting(112,224,Lucia)) &amp;&amp; (estadoDialogo == 0) {
        var texto_parcial = string_copy(textoCorsi_1, 1, floor(letras_mostradas));
    } else if (position_meeting(112,224,Lucia)) &amp;&amp; (estadoDialogo == 1) {
        var texto_parcial = string_copy(textoLucia_1, 1, floor(letras_mostradas));
    } else if (position_meeting(112,224,Lucia)) &amp;&amp; (estadoDialogo == 2) {
        var texto_parcial = string_copy(textoCorsi_2, 1, floor(letras_mostradas));
    } else if (position_meeting(112,224,Lucia)) &amp;&amp; (estadoDialogo == 3) {
        var texto_parcial = string_copy(textoCorsi_3, 1, floor(letras_mostradas));
    } else if (position_meeting(112,224,Lucia)) &amp;&amp; (estadoDialogo == 4) {
        var texto_parcial = string_copy(textoLucia_2, 1, floor(letras_mostradas));
    // ANGIE
    } else if (position_meeting(664,336,Lucia)) {
        var texto_parcial = string_copy(textoAngie_1, 1, floor(letras_mostradas));
    } else {
        var texto_parcial = " ";
    }
    draw_set_color(c_black);  // Color del texto
    draw_text(view_xview[0]+125,view_yview[0]+130, texto_parcial);
}

</string>
          </argument>
        </arguments>
      </action>
    </event>
  </events>
  <PhysicsObject>0</PhysicsObject>
  <PhysicsObjectSensor>0</PhysicsObjectSensor>
  <PhysicsObjectShape>0</PhysicsObjectShape>
  <PhysicsObjectDensity>0.5</PhysicsObjectDensity>
  <PhysicsObjectRestitution>0.100000001490116</PhysicsObjectRestitution>
  <PhysicsObjectGroup>0</PhysicsObjectGroup>
  <PhysicsObjectLinearDamping>0.100000001490116</PhysicsObjectLinearDamping>
  <PhysicsObjectAngularDamping>0.100000001490116</PhysicsObjectAngularDamping>
  <PhysicsObjectFriction>0.200000002980232</PhysicsObjectFriction>
  <PhysicsObjectAwake>-1</PhysicsObjectAwake>
  <PhysicsObjectKinematic>0</PhysicsObjectKinematic>
  <PhysicsShapePoints/>
</object>
